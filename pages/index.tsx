import { PhotoEditor } from "~/components";

const Home = () => {
    return (
        <main className="w-full h-page grid place-items-center">
            <PhotoEditor />
        </main>
    );
}

export default Home;